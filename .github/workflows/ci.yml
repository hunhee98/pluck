name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install Rust toolchain from rust-toolchain.toml
        run: rustup show

      - name: Verify version metadata
        run: python3 scripts/version-check.py --allow-unreleased

      - name: Print maintainer release status
        run: python3 scripts/maintainer-status.py

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install runtime dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ripgrep

      - name: Run tests
        run: cargo test --all

  bench-regression:
    name: bench, regression gate
    if: github.event_name == 'pull_request'
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
      issues: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Prepare benchmark output directory
        run: mkdir -p benchmarks/results

      - name: Detect core benchmark gate changes
        id: gate_changes
        shell: bash
        run: |
          set -euo pipefail

          base_sha="${{ github.event.pull_request.base.sha }}"
          head_sha="${{ github.event.pull_request.head.sha }}"

          git diff --name-only "$base_sha" "$head_sha" | tee benchmarks/results/changed-files.txt

          if grep -E '^(crates/pluck-core/|crates/pluck-mcp/src/|crates/pluck-mcp/benches/|scripts/regression-gate\.py|benchmarks/baseline\.json|Cargo\.toml|Cargo\.lock|rust-toolchain\.toml)' benchmarks/results/changed-files.txt >/dev/null; then
            echo "run_gate=true" >> "$GITHUB_OUTPUT"
          else
            echo "run_gate=false" >> "$GITHUB_OUTPUT"
            {
              echo "Benchmark and regression gate skipped."
              echo
              echo "No engine-core, MCP session, gated benchmark, baseline, Cargo, or Rust toolchain files changed."
            } > benchmarks/results/regression-gate.log
            cp benchmarks/results/regression-gate.log benchmarks/results/cargo-bench.log
          fi

      - name: Install Rust toolchain from rust-toolchain.toml
        if: steps.gate_changes.outputs.run_gate == 'true'
        run: rustup show

      - name: Cache Cargo
        if: steps.gate_changes.outputs.run_gate == 'true'
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Install runtime dependencies
        if: steps.gate_changes.outputs.run_gate == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ripgrep

      - name: Run gated benches and regression gate
        id: regression_gate
        if: steps.gate_changes.outputs.run_gate == 'true'
        shell: bash
        run: |
          set -o pipefail
          python3 scripts/regression-gate.py 2>&1 \
            | tee benchmarks/results/regression-gate.log benchmarks/results/cargo-bench.log

      - name: Collect benchmark reports
        if: always() && steps.gate_changes.outputs.run_gate == 'true'
        shell: bash
        run: |
          if [[ -d target/criterion ]]; then
            mkdir -p benchmarks/results/criterion
            cp -R target/criterion/. benchmarks/results/criterion/
          fi

      - name: Comment on regression failure
        if: failure() && steps.regression_gate.outcome == 'failure'
        uses: actions/github-script@v8
        continue-on-error: true
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- pluck-regression-gate -->';
            const logPath = 'benchmarks/results/regression-gate.log';
            const log = fs.existsSync(logPath)
              ? fs.readFileSync(logPath, 'utf8').trim()
              : 'regression-gate.log was not found.';
            const escaped = log.replaceAll('```', '\\`\\`\\`');
            const trimmed = escaped.length > 6000
              ? `${escaped.slice(-6000)}\n\n[truncated: showing last 6000 characters]`
              : escaped;
            const body = [
              marker,
              '### Regression gate failed',
              '',
              'The gated benchmark comparison against `benchmarks/baseline.json` found a regression.',
              '',
              `[View workflow run](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
              '',
              '```text',
              trimmed,
              '```',
            ].join('\n');

            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number,
              per_page: 100,
            });
            const previous = comments.find((comment) =>
              comment.user.type === 'Bot' && comment.body.includes(marker)
            );

            if (previous) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: previous.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body,
              });
            }

      - name: Upload benchmark artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: ci-benchmark-results-${{ github.run_id }}
          path: benchmarks/results/
          if-no-files-found: warn
          retention-days: 14
