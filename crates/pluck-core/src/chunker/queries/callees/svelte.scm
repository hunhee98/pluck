; Svelte callees: the JS/TS inside <script> is raw_text in this grammar,
; so call expressions are not reachable without a JS injection pass.
; Intentionally empty (the merged query still needs a callee slot).
