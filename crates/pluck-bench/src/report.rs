//! Aggregate per-run JSON into a markdown table + dashboard JSON.
//!
//! TODO: implement.

pub fn generate(_input: &str, _markdown: bool) -> anyhow::Result<()> {
    eprintln!("pluck-bench: report — not implemented yet");
    Ok(())
}
