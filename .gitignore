# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# ONNX models (downloaded by bootstrap)
/models/*.onnx
/models/*.bin

# Benchmark artifacts
/benchmarks/results/*.json
/benchmarks/repos/*/
# LLM-in-loop bench: cloned upstream repos (~1.2 GB) are ignored.
# results/*.json ARE committed (honest-reporting policy — see llm-bench/README.md).
/benchmarks/llm-bench/repos/

# Local index store
/.pluck/

# Logs
*.log

# Internal-only notes (competitor analysis, drafts, etc.) — never published.
/docs/internal/

# Internal planning roadmap (the public version arc lives in README).
/docs/ROADMAP.md

# Claude Code
CLAUDE.md
.mcp.json
.claude/

# Codex
AGENTS.md
.codex/
.agents/
